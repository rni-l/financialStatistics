// const swaggerConfig = require('../config/swagger')

/**
 * @param {Egg.Application} app - egg application
 */
module.exports = app => {
  const { router, controller } = app
  // router.get('doc', '/swagger.json', controller.other.swagger.index)
}
